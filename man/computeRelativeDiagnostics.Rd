% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spicyGLM.R
\name{computeRelativeDiagnostics}
\alias{computeRelativeDiagnostics}
\title{Compute within-pair relative leverage/influence and percentile ranks}
\usage{
computeRelativeDiagnostics(diagnostics, level = c("patient", "image"))
}
\arguments{
\item{diagnostics}{The stacked, multi-pair \code{list(pair=, patient=, image=)}
from \code{buildGLM()}/\code{combineGLM()}'s \code{computeDiagnostics = TRUE} path.}

\item{level}{\code{"patient"} or \code{"image"}; selects which of
\code{diagnostics$patient} / \code{diagnostics$image} to operate on.}
}
\value{
A data frame -- the input to \code{crossPairDiagnostics()}.
}
\description{
Rescales pre-fit leverage share and post-fit variance-share influence
against their own natural comparison scope (leverage: within-group or
within-patient; influence: pair-wide), so values become comparable across
cell-type pairs with different patient/image counts. Computes both the
leverage and influence pathways in one pass, since \code{crossPairDiagnostics()}
needs both. At \code{level = "patient"}, also ranks the point-estimate
shift \code{delta_i} pair-wide by raw \code{abs(delta_i)} -- no rescaling,
since \code{delta_i} has no natural "share of a total" baseline. There is
no image-level equivalent: no \code{delta_ij} exists to rank.
}
