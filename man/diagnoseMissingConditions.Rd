% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spicyGLM.R
\name{diagnoseMissingConditions}
\alias{diagnoseMissingConditions}
\title{Diagnose why a cell type pair has no usable data for one or more condition groups}
\usage{
diagnoseMissingConditions(
  from,
  to,
  missingConditions,
  cellTypePresence,
  context = c("spatial", "comparison")
)
}
\arguments{
\item{from, to}{Character; the reference/target cell types for this pair.}

\item{missingConditions}{Character vector of condition group(s) with no
usable rows for this pair.}

\item{cellTypePresence}{The presence table from computeCellTypePresence().}

\item{context}{"spatial" (modelDataGen) or "comparison" (buildGLM) --
only changes the closing clause of the message.}
}
\value{
A list: reason (a short code) and message (the full skip text).
}
\description{
Shared by modelDataGen() (fires before any spatial metrics exist) and
buildGLM() (fires when a specific condition group's rows are entirely
absent after the fact). Distinguishes three real causes: `from` absent,
`to` absent, or both individually present but never co-occurring in the
same image within that group.
}
