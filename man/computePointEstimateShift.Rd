% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spicyGLM.R
\name{computePointEstimateShift}
\alias{computePointEstimateShift}
\title{Compute closed-form leave-one-out shift in the fitted log rate ratio}
\usage{
computePointEstimateShift(dfResultPairwise, oneToOne, subject = NULL, GLMfit)
}
\arguments{
\item{dfResultPairwise}{Data frame for one cell-type pair, one row per
reference cell, with columns \code{n}, \code{density}, \code{condition},
\code{imageID}, and \code{subject} if \code{oneToOne} is \code{FALSE}.}

\item{oneToOne}{Logical; if \code{TRUE} cluster by \code{imageID},
otherwise by \code{subject}.}

\item{subject}{Unused; kept for signature consistency with
\code{computeLeverage()}/\code{buildGLM()}.}

\item{GLMfit}{The fitted model object for this pair, used only for the
step-5 correctness check.}
}
\value{
A data frame with columns \code{patient_id}, \code{group},
  \code{y_i}, \code{d_i}, \code{delta_i}.
}
\description{
No CR2 machinery required -- pure aggregation of raw counts/densities,
exploiting the closed-form Firth solution \code{beta_g = log((Y_g+0.5)/D_g)}.
\code{GLMfit} is used only to verify the recomputed group coefficients
against the actual fit, not for any computation.
}
